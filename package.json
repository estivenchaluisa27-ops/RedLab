{
  "name": "redlab",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=verbose",
    "build:css": "tailwindcss -i src/styles/tailwind-input.css -o styles.css --minify",
    "precommit": "npm run build:css && git add styles.css"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "tailwindcss": "^3.4.0"
  }
}
